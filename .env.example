# Chép file này thành .env rồi điền key. App tự nạp khi khởi động (assetlib.load_env).
# .env KHÔNG được đưa lên git / gửi cho người khác — key nằm nguyên văn trong đó.
GEMINI_API_KEY=
PEXELS_API_KEY=
