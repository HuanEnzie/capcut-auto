# Key nằm nguyên văn trong .env — không bao giờ đưa lên git
.env

__pycache__/

# Dữ liệu sinh ra / trạng thái của máy — không đưa lên git
shorts/work/
renders/
snapshots/
assets/user/
assets/.sfx_dai.json
library.db
*.bak
quarantine/
.venv/
capcut-auto-v1.zip
shorts/downloads/

# Đóng gói .exe (PyInstaller) — sinh ra từ dong_goi.ps1, không phải mã nguồn.
# Vài trăm MB do faster-whisper/ctranslate2 — lỡ commit là lặp lại đúng lỗi 27/07.
build/
dist/
*.spec
